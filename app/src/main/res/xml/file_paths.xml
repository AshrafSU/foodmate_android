<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For images stored in external cache -->
    <cache-path name="profile_pictures" path="."/>

    <!-- For images stored in app-specific external storage -->
    <external-files-path name="images" path="Pictures/" />
</paths>
